# Windows 下 core.autocrlf=true 会把工作树 yml 检出为 CRLF，
# 而 lefthook.yml / .pre-commit-config.yaml 的多行 `run: |` 脚本
# 每行末尾带 \r 会破坏 sh 的 if/then 结构解析（钩子误报 syntax error）。
*.yml text eol=lf
*.yaml text eol=lf
*.sh text eol=lf
